Python should support:

  * additional step argument - enumerate(something, start, step)
  * classproperty

Python 2.x doesn't seem to propagate exceptions from finally:

    try:
        stream = open("sadjflksajfdlka")
    finally:
        stream.close()

TODO: why socket doesn't work with print (print("...".encode("ascii"), file=sock)

subprocess universal_newlines best practice ???
subprocess write to file descriptor 3....


Questions:

python i: 

  * how to check for empty list (bool?)


New trainings:

  * web frameworks
  * web services + authentication
